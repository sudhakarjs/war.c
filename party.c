
#include<stdio.h>
int main()
{
int b,c,d,sum=0,f;
printf("\n enter the number of values");
scanf("%d",n);
for(b=1;b<=c;b++)
{
printf("\n enter the %d value:",b);
scanf(%d",&d);
sum=sum+d;
}
c=sum/c;
printf("\n output :",f);
return 0;
}
