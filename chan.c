#include<stdio.h>
int main()
{
int a[25],,h,p;
float median=0,sum=0;
printf("Enter the number of terms:");
scanf("%d",&h);
printf("\n Enter the number:\n");
for(p=1;p<=h;p++)
{
scanf("%d",&a[p]);
}
for(p=1;p<=h;p++)
{
for(p=1;p<=h;p++)
{
sum=sum+a[p];
avg=sum/n;
}
print("\n the median numbersare:%f",median);
return 0;
}
